---

- name: Quick test
  hosts: htpc
  tasks:
    - name: Testing Authentication
      debug:
        msg: " This is a test. PASSED!!!! "
     
    - name: Updating main server
      become: True
      shell: apt-get clean
      become_user: root
      
    - name: Additional Steps
      shell: apt-get update
      become: True
      become_user: root
